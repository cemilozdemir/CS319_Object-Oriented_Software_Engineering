{
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "express": "^4.17.3",
    "jsonwebtoken": "^8.5.1",
    "nodemon": "^2.0.16",
    "pg": "^8.7.3",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.19.0"
  },
  "scripts": {
    "migrate": "npx sequelize-cli db:migrate",
    "down-migrate": "npx sequelize-cli db:migrate:undo",
    "make-seed": "npx sequelize-cli seed:generate --name demo-data",
    "seed": "npx sequelize db:seed:all --debug",
    "down-seed": "npx sequelize-cli db:seed:undo --debug"
  },
  "devDependencies": {
    "sequelize-cli": "^6.4.1"
  }
}
